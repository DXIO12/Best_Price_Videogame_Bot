"""Application package: all of the price-bot source code.

Kept as a package so every internal import is absolute and unambiguous
(``application.gui.main_window``, ``application.shops.amazon``, ...) no matter
which entry point starts the process.
"""
